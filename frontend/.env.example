# Copy to ".env.local" for local overrides, or set this in Vercel's dashboard.
#
# Leave it UNSET locally: the Vite dev proxy forwards /api to localhost:5001.
# In production, set it to your deployed backend URL, for example:
# VITE_API_URL=https://deepfake-challenge-api.onrender.com
